import { View } from "react-native"
import { styles } from "../myStyles"
import React, { memo } from "react"
const BorderLine:React.FC =()=>{
    return<View style={styles.mainConverterComponent2}>
    </View>
}
export default memo(BorderLine)